import React from 'react';
import './Header.scss'

const Header = (props) => {
     return (
          <header>
               <img src="/img/logo.png" alt=""/>
          </header>
     )
}
 
export default Header;
